PORT=5000
JWT_SECRET=smartfarm_super_secret_key_change_in_production
OPENWEATHER_API_KEY=your_openweathermap_api_key_here

# MongoDB is NOT required — the app uses in-memory storage by default.
# Uncomment below only if you want to add MongoDB later:
# MONGODB_URI=mongodb://localhost:27017/smart-farming
